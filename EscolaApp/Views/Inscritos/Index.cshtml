@model IEnumerable<EscolaApp.Models.Inscrito>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IdTurma)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IdAluno)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CreateDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LastUpdate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IdTurma)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IdAluno)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CreateDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LastUpdate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdInscricao }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdInscricao }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdInscricao })
        </td>
    </tr>
}

</table>
